---
title: Ensembles et logique
order: 10
description: Ensembles, appartenance, quantificateurs, implications et comparaisons.
---

# Ensembles et logique

## Les ensembles des nombres et les symboles logique

Les ensembles numériques classent les nombres selon leurs propriétés.
Les symboles logiques permettent d'exprimer précisément les relations entre nombres.

### LES ENSEMBLES:
<Grid title="Familles de nombres">
  <DataTable title="Ensembles principaux" wide compact lines="rows">

    | Symbole | Nom | Exemples |
    | --- | --- | --- |
    | $\mathbb N$ | naturels | $0,1,2,3,\ldots$ |
    | $\mathbb Z$ | entiers | $\ldots,-2,-1,0,1,2,\ldots$ |
    | $\mathbb D$ | décimaux à écriture finie | $0{,}5$; $-2{,}75$; $10{,}01$ |
    | $\mathbb Q$ | rationnels | $\frac13$; $-\frac72$; $0{,}5$ |
    | $\mathbb R$ | réels | $\pi$; $\sqrt2$; $\frac13$; $-5$ |
    | $\mathbb C$ | complexes | $2+3i$ |

  </DataTable>

  <Formula title="Inclusions" wide>

  $$
  \mathbb N\subset\mathbb Z\subset\mathbb D\subset
  \mathbb Q\subset\mathbb R\subset\mathbb C
  $$

  </Formula>
</Grid>
### SYMBOLES LOGIQUES:
<Grid title="Appartenance et inclusion">
  <Definition title="Appartenance">
    $x\in A$ signifie que le nombre $x$ est un **élément** de l'ensemble $A$.
    Par exemple, $5\in\mathbb N$.
  </Definition>

  <Definition title="Inclusion">
    $A\subset B$ signifie que chaque élément de l'ensemble $A$ appartient
    aussi à l'ensemble $B$. Par exemple, $\mathbb N\subset\mathbb Z$.
  </Definition>

  <Warning title="Élément ou ensemble ?">
    On écrit **nombre $\in$ ensemble**, mais **ensemble $\subset$ ensemble**.
    Ainsi, $5\in\mathbb N$ et $\mathbb N\subset\mathbb Z$.
  </Warning>

  <Note title="Deux conventions à vérifier">
    Ici, $0\in\mathbb N$. Certains cours commencent les naturels à $1$.
    De plus, $\mathbb D$ est une notation surtout employée dans les programmes
    francophones; elle n'est pas universelle.
  </Note>
</Grid>
<Grid title="Table des symboles">
  <DataTable wide compact lines="rows">

    | Symbole | Lecture | Exemple |
    | --- | --- | --- |
    | $\in$ | appartient à | $5\in\mathbb N$ |
    | $\notin$ | n'appartient pas à | $-2\notin\mathbb N$ |
    | $\subset$ | est inclus dans | $\mathbb N\subset\mathbb Z$ |
    | $\not\subset$ | n'est pas inclus dans | $\mathbb R\not\subset\mathbb Q$ |
    | $\forall$ | pour tout | $\forall n\in\mathbb N$ |
    | $\exists$ | il existe | $\exists x\in\mathbb R$ |
    | $\nexists$ | il n'existe pas | $\nexists x\in\mathbb R:x^2=-1$ |
    | $\Rightarrow$ | implique | $x>2\Rightarrow x>1$ |
    | $\Leftrightarrow$ | équivaut à | $x^2=4\Leftrightarrow x=\pm2$ |
    | $\ne$ | différent de | $2\ne3$ |



  </DataTable>
</Grid>

<Grid title="Lire les affirmations logiques">
  <Example title="Lire une définition de suite">
    $$\forall n\in\mathbb N,\qquad u_n=2n+1$$
    se lit : « Pour tout entier naturel $n$, le terme de rang $n$ vaut
    $2n+1$. » Ici, $n$ est le rang et $u_n$ est le terme correspondant.
  </Example>

  <Definition title="Implication">
    $P\Rightarrow Q$ affirme que si $P$ est vraie, alors $Q$ est vraie.
    Cela ne garantit pas que $Q\Rightarrow P$.
  </Definition>

  <Definition title="Équivalence">
    $P\Leftrightarrow Q$ signifie que les deux implications
    $P\Rightarrow Q$ et $Q\Rightarrow P$ sont vraies.
  </Definition>

  <Warning>
    $\Rightarrow$ et $\Leftrightarrow$ ne sont pas interchangeables.
    Par exemple, $x=2\Rightarrow x^2=4$, mais $x^2=4$ n'implique pas $x=2$
    puisque $x$ peut aussi valoir $-2$.
  </Warning>
</Grid>
